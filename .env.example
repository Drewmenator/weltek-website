# Canonical origin override. Leave unset once weltekng.com points at this
# deployment. Until then set it to the live origin so OG images, canonicals
# and the sitemap resolve to a host that actually serves the site.
NEXT_PUBLIC_SITE_URL=
